<resources>

    <!-- Arama kutusu stil -->
    <style name="MyCustomTextInput" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="boxBackgroundColor">@color/card_background</item>
        <item name="hintTextColor">@color/text_secondary</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="boxStrokeColor">@color/chip_stroke</item>
        <item name="startIconTint">@color/chip_stroke</item>
        <item name="endIconTint">@color/chip_stroke</item>
    </style>

    <!-- Chip görünümü -->
    <style name="MyCustomChip" parent="Widget.MaterialComponents.Chip.Choice">
        <item name="chipBackgroundColor">@color/chip_background_selector</item>
        <item name="android:textColor">@color/chip_text_selector</item>
        <item name="chipStrokeColor">@color/coffee_chip_checked</item>
        <item name="chipStrokeWidth">1dp</item>
        <item name="chipCornerRadius">12dp</item>
    </style>




</resources>
